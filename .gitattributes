# ABOUTME: Frozen artifacts and raw data must check out byte-identical on every platform so pinned sha256 verification is environment-independent.
results/final/** binary
data/raw/challenge2014/** binary
